<h1>Hello</h1>
<% @articles.each do |article| %>
  <p><%= link_to article.title, article %></p>
  <p><%= article.body %></p>
  <%= link_to 'View Article', wiki_article_path %>
<% end %>

<%= link_to 'Create new article', new_article_path %>